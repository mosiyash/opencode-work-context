---
schema: 1
workspace: "000006"
stage: "08"
title: "Packaging and documentation"
status: "completed"
goal: "Align exports, installer-generated files, examples, README, and compatibility documentation."
prompt: "Update package exports and files, installer-generated server/TUI loaders, tui.json example, README onboarding, restart requirements, server-plugin instructions, TUI-plugin instructions, and compatibility requirements. Clearly state source of truth: core/storage stay in the package; server integration imports the server export; TUI integration imports the separate TUI export; projects do not copy src or tools; projections are never edited manually. Preserve existing server plugin behavior and existing panel setup. Add or update ADR index and durable architecture documentation where appropriate. Do not alter canonical .work-context state manually.\n\nCarry forward stage 07 findings: plugin/work-context-modal.js now exposes pure prepareWorkContextCommand(action, workspace, stage) and insertPromptCommand(api, command). Document the canonical examples /wc workspace list 000004, /wc resume 000004 02, /wc stage handoff 000004 02, and /wc stage finish 000004 02; workspace IDs must be exactly six digits and stage IDs one or two digits normalized to two digits. Modal actions are explicit keyboard actions (l/r/h/f), show Prepared (not submitted), and must never submit, invoke tools, dispatch commands, invoke an LLM, or mutate storage. Prompt insertion is best-effort only through a host-provided public append bridge when available; the stable fallback is a visible exact command plus a PROMPT_INSERT_UNAVAILABLE error instructing manual submission. The installed @opencode-ai/plugin 1.18.21 declarations expose TuiPromptRef/set but no prompt.append, so do not claim append support is confirmed; verify the target OpenCode runtime or document the fallback and any adapter needed. Keep command preparation read-only and covered by tests proving no .work-context creation.\n\nCompletion criteria: a fresh init path, package manifest, docs, and loader/config examples agree with each other; exports and generated loaders expose the modal implementation without breaking the existing sidebar/server paths; prompt insertion compatibility and fallback behavior are explicit and honest."
depends_on: ["07"]
owner: "OpenCode"
created_at: "2026-09-01T12:08:19.356Z"
updated_at: "2026-09-01T12:28:48.613Z"
tracker_links: []
---



# Packaging and documentation

## Goal

Align exports, installer-generated files, examples, README, and compatibility documentation.

## Prompt

Update package exports and files, installer-generated server/TUI loaders, tui.json example, README onboarding, restart requirements, server-plugin instructions, TUI-plugin instructions, and compatibility requirements. Clearly state source of truth: core/storage stay in the package; server integration imports the server export; TUI integration imports the separate TUI export; projects do not copy src or tools; projections are never edited manually. Preserve existing server plugin behavior and existing panel setup. Add or update ADR index and durable architecture documentation where appropriate. Do not alter canonical .work-context state manually.

Carry forward stage 07 findings: plugin/work-context-modal.js now exposes pure prepareWorkContextCommand(action, workspace, stage) and insertPromptCommand(api, command). Document the canonical examples /wc workspace list 000004, /wc resume 000004 02, /wc stage handoff 000004 02, and /wc stage finish 000004 02; workspace IDs must be exactly six digits and stage IDs one or two digits normalized to two digits. Modal actions are explicit keyboard actions (l/r/h/f), show Prepared (not submitted), and must never submit, invoke tools, dispatch commands, invoke an LLM, or mutate storage. Prompt insertion is best-effort only through a host-provided public append bridge when available; the stable fallback is a visible exact command plus a PROMPT_INSERT_UNAVAILABLE error instructing manual submission. The installed @opencode-ai/plugin 1.18.21 declarations expose TuiPromptRef/set but no prompt.append, so do not claim append support is confirmed; verify the target OpenCode runtime or document the fallback and any adapter needed. Keep command preparation read-only and covered by tests proving no .work-context creation.

Completion criteria: a fresh init path, package manifest, docs, and loader/config examples agree with each other; exports and generated loaders expose the modal implementation without breaking the existing sidebar/server paths; prompt insertion compatibility and fallback behavior are explicit and honest.


## Result

Aligned packaging and documentation: the package now exposes the combined read-only TUI entry at opencode-work-context/tui while preserving separate server exports; init generates server and TUI loaders with package imports; README and ADR documentation describe source-of-truth boundaries, canonical /wc examples, restart requirements, and honest prompt insertion fallback behavior. Added the ADR index, exposed the work-context snapshot export, fixed modal refresh/render behavior, and updated the export contract test. Verification: npm test passed with 81 passing and 2 skipped; npm pack --dry-run included the runtime package files.
