---
schema: 1
workspace: "000009"
stage: "03"
title: "Build new release"
status: "completed"
goal: "Build new release"
prompt: "Prepare the next package release. Inspect the current version, changelog/release conventions, and working tree before making changes. Choose the appropriate semantic version bump based on the completed leader-key correction, update release documentation if the repository convention requires it, and verify the package contents. Run npm test, npm run check:language, npm pack --dry-run, and working-tree checks. Do not run npm publish locally; releases must use .github/workflows/release.yml with npm Trusted Publishing via GitHub Actions OIDC. Report the selected version, exact changed files, exact command results, and any remaining release steps. Never manually edit .work-context Markdown, JSONL, INDEX, or SESSIONS projections."
depends_on: []
owner: "OpenCode"
created_at: "2026-09-04T04:16:17.414Z"
updated_at: "2026-09-04T04:17:41.984Z"
tracker_links: []
---



# Build new release

## Goal

Build new release

## Prompt

Prepare the next package release. Inspect the current version, changelog/release conventions, and working tree before making changes. Choose the appropriate semantic version bump based on the completed leader-key correction, update release documentation if the repository convention requires it, and verify the package contents. Run npm test, npm run check:language, npm pack --dry-run, and working-tree checks. Do not run npm publish locally; releases must use .github/workflows/release.yml with npm Trusted Publishing via GitHub Actions OIDC. Report the selected version, exact changed files, exact command results, and any remaining release steps. Never manually edit .work-context Markdown, JSONL, INDEX, or SESSIONS projections.


## Result

Prepared patch release 0.1.13 for the completed leader-key correction. Updated package.json and package-lock.json from 0.1.12 to 0.1.13; no changelog update was required because the repository has no changelog convention. Verification passed: npm test (114 passed, 2 skipped, 0 failed); npm run test:tui (62 passed, 0 failed); npm run check:language (165 files inspected); npm pack --dry-run (opencode-work-context-0.1.13.tgz, 49 files); git diff --check (passed); git status confirmed only the intended correction, release metadata, and work-context projections are present. No commit, tag, push, or npm publish was performed. Remaining release steps are to commit the changes, push main, push tag v0.1.13, monitor the GitHub Actions release workflow, and verify npm version 0.1.13 and the latest dist-tag.
