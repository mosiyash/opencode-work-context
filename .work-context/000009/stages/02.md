---
schema: 1
workspace: "000009"
stage: "02"
title: "Restore default leader key"
status: "completed"
goal: "Restore default leader key"
prompt: "Implement the planned leader-key correction. In bin/opencode-work-context.js, stop generating a keybinds.leader override so OpenCode's native default leader remains active. Update the repository .opencode/tui.json fixture/config to omit the leader override, update installer expectations and any TUI shortcut tests so they verify <leader> bindings rather than claiming Ctrl+Alt+W is the global leader, and update English README guidance and shortcut labels from Ctrl+Alt+W to the default Ctrl+X prefix where applicable. Preserve the plugin's commands and <leader>w/s/o/e bindings; do not replace them with hard-coded ctrl+x sequences. Do not manually edit any .work-context Markdown, JSONL, INDEX, or SESSIONS projections. Run npm test and npm run check:language, then report the exact result and changed files."
depends_on: ["01"]
owner: "OpenCode"
created_at: "2026-09-03T11:33:16.930Z"
updated_at: "2026-09-04T03:21:39.796Z"
tracker_links: []
---



# Restore default leader key

## Goal

Restore default leader key

## Prompt

Implement the planned leader-key correction. In bin/opencode-work-context.js, stop generating a keybinds.leader override so OpenCode's native default leader remains active. Update the repository .opencode/tui.json fixture/config to omit the leader override, update installer expectations and any TUI shortcut tests so they verify <leader> bindings rather than claiming Ctrl+Alt+W is the global leader, and update English README guidance and shortcut labels from Ctrl+Alt+W to the default Ctrl+X prefix where applicable. Preserve the plugin's commands and <leader>w/s/o/e bindings; do not replace them with hard-coded ctrl+x sequences. Do not manually edit any .work-context Markdown, JSONL, INDEX, or SESSIONS projections. Run npm test and npm run check:language, then report the exact result and changed files.


## Result

Stage completed.
