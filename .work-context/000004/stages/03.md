---
schema: 1
workspace: "000004"
stage: "03"
title: "Show concise stage information on resume"
status: "completed"
goal: "Show concise stage information on resume"
depends_on: []
owner: "OpenCode"
created_at: "2026-08-31T09:01:05.239Z"
updated_at: "2026-08-31T09:56:52.571Z"
tracker_links: []
prompt: "Study /wc resume behavior and improve the UX: on resume, the agent must first show the user a concise structured stage context (the stage essence, what was done or where the previous session stopped, and what is planned now), and only then begin work. Update the structured result of work_context_start_session and the commands/wc.md contract so this context is clearly shown to the user before any changes. Do not begin implementation when the prompt is empty or questions remain unresolved; return ask_questions in those cases, make no code changes, and ask the user focused questions. Preserve the existing resume fields and backward-compatible behavior for stages with non-empty prompts. Add regression tests for the concise context and empty-prompt block. Run npm test; if the installer tests fail again because of fake npm under Node 26, treat that as an external limitation."
---

# Show concise stage information on resume

## Goal

Show concise stage information on resume

## Prompt

Study /wc resume behavior and improve the UX: on resume, the agent must first show the user a concise structured stage context (the stage essence, what was done or where the previous session stopped, and what is planned now), and only then begin work. Update the structured result of work_context_start_session and the commands/wc.md contract so this context is clearly shown to the user before any changes. Do not begin implementation when the prompt is empty or questions remain unresolved; return ask_questions in those cases, make no code changes, and ask the user focused questions. Preserve the existing resume fields and backward-compatible behavior for stages with non-empty prompts. Add regression tests for the concise context and empty-prompt block. Run npm test; if the installer tests fail again because of fake npm under Node 26, treat that as an external limitation.
## Result

Stage completed.
